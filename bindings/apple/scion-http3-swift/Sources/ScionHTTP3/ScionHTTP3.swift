// Copyright 2026 Anapaya Systems

// The generated bindings are an implementation detail of this module. The access level on the
// import makes the compiler refuse a generated type in a public signature, so nothing generated can
// reach an adopter through this module.
internal import ScionHTTP3Uniffi

// The public API of the SCION HTTP/3 client for Apple platforms arrives with the facade issue. Until
// then this module is empty on purpose: it fixes the module name and the dependency edge that the
// facade is written against.

{
    "name": "@anapaya/docusaurus-code-import",
    "version": "0.0.0",
    "private": true,
    "description": "remark plugin: include code samples by reference (named anchors, line ranges, @sdk alias) into Docusaurus/MDX guide pages, so snippets are pulled from real tested sources instead of copy-pasted.",
    "license": "Apache-2.0",
    "type": "commonjs",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": ["dist"],
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "prepare": "tsc -p tsconfig.json"
    },
    "devDependencies": {
        "typescript": "~5.6.2"
    }
}

# SCION SDK docs — local preview

A minimal, standalone Docusaurus app for previewing the SCION SDK developer
guide **from within this repo**.

```bash
pnpm install
pnpm start          # http://localhost:3000  (live reload)
```
